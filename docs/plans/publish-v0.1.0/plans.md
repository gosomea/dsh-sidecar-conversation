# v0.1.0 发布计划

目标是把当前 Better Sidebar + native Conversation Surface 实现作为首个 npm 版本发布，同时准确声明它对 Harness 源码扩展的依赖，不把“可安装”误写成“兼容未打补丁的官方 Web”。

主线：冻结代码与文档 → 独立构建/测试/打包 → npm 官方 registry 发布 → 验证公开元数据与安装 → 创建 Git 标签并推送 GitHub。

